version: "3.9"
services:
  api:
    build: .
    command: uvicorn src.main:create_app --factory --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
    volumes:
      - .:/app
    restart: unless-stopped
